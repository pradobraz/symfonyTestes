{% extends 'base.html.twig' %}

{% block title %}Clone Usuarios{% endblock %}

{% block body %}
<div class="container">
        <div class="row">
            <h1>Clone Usuarios</h1>
        </div>
        <div class="row">
            <div class="col-md-10">
              {{ include('usuarios/_form.html.twig', {'button_label': 'Clone'}) }}

              {{ include('usuarios/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
    
    <!--<a href="{{ path('usuarios_index') }}">back to list</a>-->

    

{% endblock %}
